/**
* @file PlayerComponent.hpp
 * @brief Component that marks an entity as the player.
 *
 * The PlayerComponent is a tag component that identifies
 * the player entity for special handling.
 */
#ifndef PLAYERCOMPONENT_HPP
#define PLAYERCOMPONENT_HPP

struct PlayerComponent
{
    PlayerComponent() = default;
};

#endif //PLAYERCOMPONENT_HPP